# Supabase
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Sól do hashowania haseł — wygeneruj losowy string np.:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# WAŻNE: zmiana tej wartości unieważnia wszystkie hasła w bazie!
VITE_PASSWORD_SALT=your-random-salt-here

# Tylko dla skryptu create_users.js (service_role key — nigdy nie commituj!)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here